@model RegisterViewModel

<div class="container">
    <form asp-controller="Account" asp-action="Register" method="post" class="col s12" style="max-width: 500px" role="form">
        <h3 class="header">Create a new account.</h3>
        <div class="row brown lighten-1">
            <div class="input-field col s12">
                <i class="material-icons prefix">account_circle</i>
                <input asp-for="Email" id="email" type="email" class="validate">
                <label class="black-text" for="email">Email</label>
            </div>
        </div>
        <div class="row brown lighten-1">
            <div class="input-field col s12">
                <i class="material-icons prefix">vpn_key</i>
                <input asp-for="Password" id="password" type="password" class="validate">
                <label class="black-text" for="password">Password</label>
            </div>
        </div>
        <div class="row brown lighten-1">
            <div class="input-field col s12">
                <i class="material-icons prefix">vpn_key</i>
                <input asp-for="ConfirmPassword" id="password" type="password" class="validate">
                <label class="black-text" for="password">Confirm password</label>
            </div>
        </div>
        <div>
            <button class="waves-effect waves-light btn" type="submit">Sign in</button>
        </div>

        <div asp-validation-summary="ValidationSummary.All" class="row"></div>
    </form>
</div>

@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}
